config = {
    # if set, show the plot by default in the convenience functions
    'show': False,

    # if set, the convenience functions put the plot into
    # the background by default
    'fork_window': False,
}
